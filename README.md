# Noodles-Interplay


create the venv:
python3 -m venv venv

activate the venv:
  Windows:
    Scripts\activate.bat

  Unix based systems
    Source /bin/activate

install packages:
python3 -m pip install -r requirements.txt

